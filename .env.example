NODE_ENV=development

# Core database connection (PostgreSQL)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"

# NextAuth / Auth.js base URL (update per environment)
NEXTAUTH_URL="http://localhost:3000"

# NextAuth secret (generate securely in real envs)
NEXTAUTH_SECRET="replace-with-a-long-random-string"

# Optional: email provider, OAuth, etc. (placeholders for later)
# EMAIL_SERVER=smtp://user:pass@smtp.mailtrap.io:2525
# EMAIL_FROM="Dinevora <no-reply@dinevora.local>"

